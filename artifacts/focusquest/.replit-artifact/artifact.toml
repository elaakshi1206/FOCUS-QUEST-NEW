kind = "web"
previewPath = "/"
title = "FocusQuest"
version = "1.0.0"
id = "artifacts/focusquest"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23966

[services.development]
run = "pnpm --filter @workspace/focusquest run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/focusquest", "run", "build" ]
publicDir = "artifacts/focusquest/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23966"
BASE_PATH = "/"
